{
  "name": "@nextjs-sidecar/shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
